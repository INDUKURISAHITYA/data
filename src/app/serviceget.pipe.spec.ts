import { ServicegetPipe } from './serviceget.pipe';

describe('ServicegetPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicegetPipe();
    expect(pipe).toBeTruthy();
  });
});
