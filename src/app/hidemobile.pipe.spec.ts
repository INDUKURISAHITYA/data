import { HidemobilePipe } from './hidemobile.pipe';

describe('HidemobilePipe', () => {
  it('create an instance', () => {
    const pipe = new HidemobilePipe();
    expect(pipe).toBeTruthy();
  });
});
